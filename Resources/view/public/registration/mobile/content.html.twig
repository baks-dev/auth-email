{% trans_default_domain 'public.reg' %}

<main class="main d-print-none">

    <div class="mt-5">
        {# {{ include(_self|replace({ "content.html.twig": "breadcrumb.html.twig" })) }} #}
    </div>

    <div class="container d-flex flex-column justify-content-center w-100 my-5 pb-3">


        <div class="d-flex gap-3 mb-4 align-items-center small">
            <h3 class="modal-title text-uppercase h4 fw-bold w-75">
                {{ 'page'|trans }}
            </h3>
            <a href="{{ path('auth-email:public.login') }}" class="small py-1"
               aria-label="{{ 'link.breadcrumb.page'|trans({}, 'public.login') }}"
               title="{{ 'link.breadcrumb.page'|trans({}, 'public.login') }}"
            >
                {{ 'link.breadcrumb.page'|trans({}, 'public.login') }}
            </a>
        </div>


        <div class="w-100">
            {# ФОРМА РЕГИСТРАЦИИ #}
            {{ include(_self|replace({ "content.html.twig": "_form.html.twig" })) }}
        </div>

    </div>

</main>


